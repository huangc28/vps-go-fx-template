# Copy to .env and adjust as needed.

APP_NAME=go-vps-service
APP_ENV=development
APP_PORT=8080
APP_SHUTDOWN_TIMEOUT=10s
LOG_LEVEL=info

# Optional Postgres (disabled when unset)
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=5432
DB_NAME=
